Automatically deploys a wireguard tunnel on any Linux VPS, then sends your config to a Discord webhook.

Usage: 
```
git clone https://github.com/zzylos/wireguard-autodeploy/ 
cd wireguard-autodeploy 
chmod +x wireguard-autosetup.sh 
sudo ./wireguard-autosetup.sh "YOUR_WEBHOOK_HERE"
```
